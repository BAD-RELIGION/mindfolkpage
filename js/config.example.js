// Mindfolk site runtime configuration — EXAMPLE FILE (safe to commit).
// Copy to js/config.js and fill in your real Helius API key.
// js/config.js is git-ignored so the key stays off GitHub.
window.CONFIG = {
  HELIUS_API_KEY: 'PASTE_YOUR_HELIUS_API_KEY_HERE',
};
